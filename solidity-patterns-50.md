# Solidity 漏洞模式库 — 18 → 50+

## 你有 ✅ (18)

### Web 通用 (16)
1. 路径遍历 → open() 无验证
2. SQL 注入 → f-string 拼接
3. SSRF → 用户 URL 无过滤
4. 命令注入 → shell=True
5. 硬编码密钥
6. CORS 全放通
7. eval/exec 无沙箱
8. 反序列化
9. Prototype Pollution
10. NoSQL 注入
11. TLS 验证禁用
12. 弱随机数
13. OS 命令注入
14. 整数溢出
15. 认证缺失
16. URL 解析偏移

### Solidity (18 → 已有模式)
1. 重入攻击 (Reentrancy)
2. 整数溢出 (<0.8)
3. 未授权访问 (Ownable 缺失)
4. 自毁 (SELFDESTRUCT)
5. 预言机价格操纵
6. 闪电贷攻击
7. 紧急停止缺失
8. 合约未初始化
... (10 个基础)

## 需要补充 ✅ (32)

### 核心 DeFi (10) — 最高价值
```
19. TWAP 预言机操纵 — 利用时间加权平均价格的计算偏差
20. 滑点保护不足 — swap 中的 frontrunning/三明治
21. 通缩代币兼容 — fee-on-transfer 导致账面≠实际
22. ERC-4626 通胀攻击 — 共享/存款比例操纵
23. 奖励计算操纵 — 质押奖励的时间权重错误
24. 清算阈值操纵 — 借贷协议的健康因子绕过
25. 闪电贷 + 价格操纵组合 — 多步攻击的经典模式
26. 重入 + 闪电贷组合 — 递归调用 + 大额资金
27. 流动性池操纵 — 单边添加/移除导致的不平衡
28. 治理攻击 — 投票权重 / 提案执行漏洞
```

### MEV (5) — 中高价值
```
29. 三明治攻击 — 前置交易 + 后置交易
30. 抢跑交易 (Frontrunning) — 交易顺序依赖
31. JIT 流动性 — 临时添加池子提取收益
32. 时间戳操纵 — block.timestamp 依赖
33. 批量操作排序 — 交易池内的顺序操纵
```

### 跨链/桥 (5) — 高价值
```
34. 验证器签名伪造 — 跨链消息的签名绕过
35. 重放攻击 — 同一条消息在多个链重复提现
36. 轻客户端欺骗 — 验证跨链头信息的漏洞
37. 中继器操纵 — 跨链消息传递者作恶
38. 存款/铸币不匹配 — 跨链金额映射漏洞
```

### Token 标准 (5) — 基础但重要
```
39. ERC-20 approve 竞争 — frontrunning approve
40. ERC-721 重入 — safeTransferFrom 回调
41. ERC-1155 批量操作 — batch 函数的重入
42. Permit 签名钓鱼 — 离线签名的 Phishing
43. 捐赠攻击 — 向池子直接转账打破会计假设
```

### 高级 (7) — 复杂协议
```
44. 再平衡攻击 — AMM 池权重调整时的套利
45. 利率模型操纵 — 借贷利率计算公式攻击
46. 费率精度损失 — 除法截断导致循环提现
47. CREATE2 地址碰撞 — 提前部署合约到预期地址
48. delegatecall 代理 — 存储布局冲突
49. 自毁合约 — 通过 destroy 改变合约地址
50. storage 碰撞 — UUPS 代理的存储槽冲突
```

## 学习顺序

```
第 1 步: 19-28 (DeFi 核心) — 能审 80% 的 DeFi 协议
第 2 步: 39-43 (Token 标准) — 能审代币合约  
第 3 步: 34-38 (跨链) — 能审跨链桥
第 4 步: 29-33 (MEV) — 进阶
第 5 步: 44-50 (高级) — 专家级
```

## 每个模式学什么

以 **闪电贷 + 价格操纵 (25)** 为例：

```solidity
// 漏洞示例
function getPrice() public view returns (uint) {
    // ❌ 使用当前池子的瞬时价格
    return pool.getReserves();  // 可被闪电贷操纵
}

// 修复示例
function getPrice() public view returns (uint) {
    // ✅ 使用 TWAP 价格
    return twapOracle.consult(address(this), 1e18);
}
```

**判断标准：** 代码是否假设了"外部输入不会被操纵"。闪电贷可以在一个交易内借出巨量资金、扭曲价格、利用扭曲后的价格获利、归还闪电贷。如果合约任何地方用了瞬时价格作为关键参数——这就是漏洞。
